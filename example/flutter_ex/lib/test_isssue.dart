import 'package:flutter/material.dart';
import 'example_reader.dart';


class TestIssue extends StatelessWidget {
  const TestIssue({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Center(
            child: const Text('Hello'),
          )
        ],
      ),
    );
  }
}
